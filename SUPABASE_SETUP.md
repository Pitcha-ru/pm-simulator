# 🗄️ Настройка Supabase Storage

Пошаговая инструкция с скриншотами по настройке хранилища для игры.

## Шаг 1: Создание проекта

1. Перейдите на https://supabase.com/dashboard
2. Нажмите **"New project"**
3. Заполните форму:
   - **Name:** `pm-simulator` (или любое другое)
   - **Database Password:** (создайте надежный пароль)
   - **Region:** выберите ближайший регион
   - **Pricing Plan:** Free (достаточно для игры)
4. Нажмите **"Create new project"**
5. Подождите ~2 минуты пока проект создается

## Шаг 2: Создание Storage Bucket

1. В левом меню выберите **"Storage"** (иконка папки)
2. Нажмите зеленую кнопку **"Create a new bucket"**
3. В форме:
   - **Name:** `pm-sim-assets`
   - **Public bucket:** ✅ **Включите этот чекбокс** (важно!)
   - **File size limit:** оставьте по умолчанию
   - **Allowed MIME types:** оставьте по умолчанию
4. Нажмите **"Create bucket"**

### ⚠️ Важно: Bucket должен быть PUBLIC!

Если bucket не публичный, игра не сможет загружать файлы!

Проверить статус:
- В списке buckets должна быть иконка 🌐 или надпись "Public"
- Если bucket приватный, нажмите на него > Settings > **Make public**

## Шаг 3: Создание структуры папок

1. Откройте bucket `pm-sim-assets`
2. Нажмите **"Create folder"**
3. Введите имя: `game`
4. Нажмите **"Create"**
5. Войдите в папку `game/`
6. Создайте подпапки (повторяйте для каждой):
   - `dep-icons`
   - `priority-icons`
   - `authors-icons`

Итоговая структура:
```
pm-sim-assets/
└── game/
    ├── dep-icons/
    ├── priority-icons/
    └── authors-icons/
```

## Шаг 4: Загрузка файлов

### Вариант A: Через веб-интерфейс

#### 4.1 Загрузка JSON конфигов

1. Откройте папку `game/`
2. Нажмите **"Upload"** > **"Upload file"**
3. Выберите файлы (можно multiple select):
   - `config.json`
   - `departments.json`
   - `task_authors.json`
   - `priority_comments.json`
4. Нажмите **"Upload"**

#### 4.2 Загрузка иконок департаментов

1. Откройте папку `game/dep-icons/`
2. Нажмите **"Upload"** > **"Upload file"**
3. Выберите все файлы из локальной папки `dep-icons/`:
   - `icon_backend.png`
   - `icon_ban_team.png`
   - `icon_casino.png`
   - `icon_lk.png`
   - `icon_o_team.png`
   - `icon_payments.png`
   - `icon_rules.png`
   - `icon_seo.png`
   - `icon_sys_admins.png`
   - `icon_web.png`
   - `icon_welcome.png`
4. Нажмите **"Upload"**

#### 4.3 Загрузка иконок приоритетов

1. Откройте папку `game/priority-icons/`
2. Загрузите файлы из `priority-icons/`:
   - `critical.png`
   - `regular.png`
   - `serious.png`

#### 4.4 Загрузка аватаров

1. Откройте папку `game/authors-icons/`
2. Загрузите файлы из `authors-icons/`:
   - `avatar_anna.png`
   - `avatar_dmitry.png`
   - `avatar_ekaterina.png`
   - `avatar_igor.png`
   - `avatar_maria.png`

### Вариант B: Через Supabase CLI (быстрее)

```bash
# Установите Supabase CLI
npm install -g supabase

# Авторизуйтесь
supabase login

# Используйте готовый скрипт
chmod +x upload-to-supabase.sh
./upload-to-supabase.sh
```

## Шаг 5: Проверка загрузки

Убедитесь что в `pm-sim-assets/game/` есть:
- ✅ 4 JSON файла
- ✅ Папка `dep-icons/` с 11 иконками
- ✅ Папка `priority-icons/` с 3 иконками
- ✅ Папка `authors-icons/` с 5 аватарами

**Общее:** 23 файла

## Шаг 6: Получение credentials

Эти данные нужны для Cloudflare Pages:

1. Перейдите в **Settings** (⚙️ в левом меню)
2. Выберите **API**
3. Найдите секцию **Project API keys**
4. Скопируйте:
   - **Project URL:** `https://xxxxxxxxxxxxx.supabase.co`
   - **anon/public key:** (длинный JWT токен, начинается с `eyJ...`)

### ⚠️ Какой ключ использовать?

Используйте **anon** (или **public**) ключ - это **безопасно**!
- ❌ НЕ используйте `service_role` ключ (это секретный серверный ключ)
- ✅ Используйте `anon` ключ (он предназначен для публичного использования)

## Шаг 7: Тестирование

Проверьте доступность файла напрямую в браузере:

```
https://YOUR_PROJECT_ID.supabase.co/storage/v1/object/public/pm-sim-assets/game/config.json
```

Замените `YOUR_PROJECT_ID` на ваш ID из Project URL.

Если файл открывается - всё настроено правильно! ✅

## 📝 Редактирование файлов после деплоя

### Через веб-интерфейс

1. Откройте Supabase Dashboard > Storage > `pm-sim-assets/game/`
2. Найдите нужный файл (например, `config.json`)
3. Нажмите три точки **"..."** рядом с файлом
4. Выберите **"Download"**
5. Отредактируйте файл локально
6. Вернитесь в Storage и нажмите **"Upload"**
7. Выберите обновленный файл
8. Подтвердите замену (Replace existing file)

### Через CLI

```bash
# Скачать файл
supabase storage cp supabase://pm-sim-assets/game/config.json ./config.json

# Отредактировать файл локально
# vim config.json или в любом редакторе

# Загрузить обратно
supabase storage cp ./config.json supabase://pm-sim-assets/game/config.json
```

## 🔧 Troubleshooting

### Ошибка: "Bucket not found"
- Убедитесь что bucket называется именно `pm-sim-assets`
- Проверьте что bucket создан в правильном проекте

### Ошибка: "Access denied" при загрузке в игре
- Bucket должен быть **Public**
- Проверьте: Storage > pm-sim-assets > Settings > Make public

### Файлы загружены, но не видны в игре
- Проверьте структуру папок: файлы должны быть в `game/`, а не в корне bucket
- Проверьте имена файлов (они чувствительны к регистру!)
- Очистите кэш браузера (Ctrl+Shift+R)

### Иконки не отображаются
- Проверьте что PNG файлы находятся в правильных подпапках
- Имена файлов в JSON должны совпадать с именами в Storage
- Убедитесь что изображения действительно в формате PNG

## 📊 Квоты Free Plan

Бесплатный план Supabase включает:
- ✅ 1 GB Storage
- ✅ 50 GB Bandwidth/месяц
- ✅ Unlimited requests

Этого более чем достаточно для игры!

Размер всех файлов игры: ~2 MB
Типичный трафик: ~10-20 GB/месяц при 1000-2000 игроков

## ✨ Готово!

Теперь ваши конфиги в облаке и вы можете редактировать их без редеплоя игры!

**Следующий шаг:** [Деплой на Cloudflare Pages →](./DEPLOYMENT.md#часть-2️⃣-деплой-на-cloudflare-pages)

