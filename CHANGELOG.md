# Changelog - Мультикарточный режим

## Версия 1.2.0 - Мультикарточный режим

### 🎮 Основные изменения

#### 1. **Мультикарточный режим**
- Теперь на экране может быть несколько карточек одновременно (до 5)
- Карточки появляются через определенные интервалы
- Каждая карточка имеет свой независимый таймер

#### 2. **Исправлен Drag & Drop**
- Карточка теперь **остается на месте** при перетаскивании
- Создается **клон карточки**, который следует за курсором
- Оригинальная карточка остается видимой (с прозрачностью 50%)
- После drop клон удаляется

#### 3. **Система жизней и Game Over**
- У игрока есть **3 жизни** (отображаются как ❤️)
- Каждая пропущенная задача (timeout) отнимает **1 жизнь**
- После потери всех жизней → **Game Over экран**
- Game Over показывает финальную статистику

#### 4. **Новая механика сложности**
- **Время на задачу** теперь **постоянное** (25 секунд для regular)
- **Сложность растет** через **уменьшение интервала spawn**
- Уровень 1: новая карточка каждые 8 секунд
- Уровень 10: новая карточка каждые ~3.5 секунд
- Уровень 20+: новая карточка каждые ~2 секунды

---

## 📊 Изменения в конфигурации

### config.json

#### Добавлено:

```json
{
  "lives": {
    "startLives": 3,
    "maxLives": 3
  },
  
  "multiCard": {
    "enabled": true,
    "maxSimultaneousCards": 5
  },
  
  "spawnInterval": {
    "baseSeconds": 8,
    "perLevelScaling": {
      "mode": "exponential",
      "multiplierEachLevel": 0.92,
      "minSeconds": 2
    }
  }
}
```

#### Изменено:

```json
{
  "timers": {
    "regular": {
      "baseSeconds": 25,  // Было 18
      "perLevelScaling": {
        "mode": "constant",  // Было "exponential"
        "keepConstant": true
      }
    }
  }
}
```

---

## 🎨 Изменения в интерфейсе

### Добавлено:
- **Счетчик жизней** в header (❤️ 3)
- **Game Over экран** с финальной статистикой
- **Множественные карточки** на доске
- **Анимация spawn** для новых карточек
- **Клон карточки** при drag

### Изменено:
- Карточки теперь меньше (360px вместо 420px)
- Карточки размещаются в flex-контейнере
- Улучшена анимация появления карточек

---

## 💻 Изменения в коде

### gameState.js
- ✅ Переписан для поддержки массива `activeTasks[]`
- ✅ Добавлена система жизней
- ✅ Добавлен метод `createTask()` вместо `spawnTask()`
- ✅ Добавлены методы работы с множественными задачами
- ✅ Добавлен `canSpawnTask()` для проверки лимита

### dragDrop.js
- ✅ Полностью переписан для клонирования
- ✅ Создается клон карточки при drag
- ✅ Оригинал остается на месте
- ✅ Добавлен метод `cancelDrag()` для cleanup

### ui.js
- ✅ Добавлены методы для работы с множественными карточками
- ✅ `addTaskCard()` - добавить карточку
- ✅ `removeTaskCard()` - удалить карточку
- ✅ `updateTaskTimer()` - обновить таймер конкретной карточки
- ✅ Добавлен `showGameOver()` для экрана Game Over

### main.js
- ✅ Добавлен `startSpawnLoop()` - интервал spawn
- ✅ `updateAllTimers()` - обновляет все карточки
- ✅ `checkTimeouts()` - проверяет просроченные
- ✅ `onTaskTimeout()` - обработка timeout с потерей жизни
- ✅ `onGameOver()` - экран Game Over

### gameLogic.js
- ✅ Добавлен `calculateSpawnInterval()` - расчет интервала
- ✅ Обновлен `calculateTimerDuration()` - поддержка constant mode

---

## 🎯 Игровой процесс

### Старая механика:
1. Одна карточка на экране
2. Время уменьшается с уровнями
3. Бесконечные попытки
4. Только streak сбрасывается при ошибке

### Новая механика:
1. До 5 карточек одновременно
2. Время постоянное (25 сек)
3. 3 жизни, Game Over при потере всех
4. Интервал spawn уменьшается с уровнями
5. Карточка остается на месте при drag

---

## 📈 Формулы

### Spawn Interval
```
interval = baseSeconds * (0.92 ^ (level - 1))
interval = max(minSeconds, interval)
```

Примеры:
- Уровень 1: 8.0s
- Уровень 5: 5.8s
- Уровень 10: 3.5s
- Уровень 15: 2.1s
- Уровень 20+: 2.0s (минимум)

### Timer Duration (constant)
```
duration = baseSeconds * priorityMultiplier
```

Примеры:
- Regular: 25.0s
- Serious: 21.25s (25 * 0.85)
- Critical: 17.5s (25 * 0.70)

---

## 🎮 Балансировка

### Рекомендуемые параметры

**Легкая сложность:**
```json
{
  "lives": { "startLives": 5 },
  "timers": { "regular": { "baseSeconds": 30 } },
  "spawnInterval": { "baseSeconds": 10, "minSeconds": 3 }
}
```

**Средняя сложность (текущая):**
```json
{
  "lives": { "startLives": 3 },
  "timers": { "regular": { "baseSeconds": 25 } },
  "spawnInterval": { "baseSeconds": 8, "minSeconds": 2 }
}
```

**Высокая сложность:**
```json
{
  "lives": { "startLives": 3 },
  "timers": { "regular": { "baseSeconds": 20 } },
  "spawnInterval": { "baseSeconds": 6, "minSeconds": 1.5 }
}
```

---

## 🐛 Исправленные проблемы

- ✅ Карточка больше не "прыгает" при клике
- ✅ Карточка остается на месте при drag
- ✅ Добавлена четкая система Game Over
- ✅ Множественные карточки работают корректно
- ✅ Таймеры работают независимо

---

## 🚀 Обновление без релиза

Теперь через `config.json` можно менять:
- ✅ Количество жизней
- ✅ Максимум одновременных карточек
- ✅ Интервал spawn
- ✅ Базовое время на задачу
- ✅ Минимальный интервал spawn
- ✅ Режим масштабирования (constant/exponential)

---

## 📝 Breaking Changes

⚠️ **Внимание**: Эта версия изменяет core механику игры.

Если у вас были сохранения или метрики старой версии:
- Статистика несовместима (другая механика сложности)
- Config файлы нужно обновить (новые поля обязательны)
- HTML требует обновления (новые элементы)

---

## 🔄 Миграция

### Если обновляете существующий проект:

1. **Замените config.json** - добавьте новые секции
2. **Обновите все JS файлы** - новая логика
3. **Обновите index.html** - новые элементы
4. **Обновите styles.css** - новые стили

Или просто используйте новые версии файлов целиком.

---

**Версия**: 1.2.0  
**Дата**: 29 декабря 2025  
**Schema Version**: 1.2.0

