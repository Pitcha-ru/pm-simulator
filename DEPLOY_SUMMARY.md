# 📦 Итоговая инструкция по деплою

Игра полностью готова к деплою на Cloudflare Pages с Supabase Storage!

## ✅ Что уже готово

- ✅ Production build работает корректно
- ✅ Все конфигурационные файлы на месте
- ✅ Система загрузки из Supabase настроена
- ✅ Fallback на локальные файлы для разработки
- ✅ Скрипт автоматической загрузки в Supabase
- ✅ Полная документация

## 📋 Что вам нужно сделать

### Вариант 1: Следовать пошаговой инструкции

Откройте и следуйте одному из этих файлов (на выбор):

1. **[QUICK_START.md](./QUICK_START.md)** - самый короткий путь
2. **[DEPLOY_CHECKLIST.md](./DEPLOY_CHECKLIST.md)** - быстрый чеклист
3. **[DEPLOYMENT.md](./DEPLOYMENT.md)** - подробная инструкция

### Вариант 2: Автоматическая загрузка в Supabase

Если уже создали проект в Supabase:

```bash
# Установите Supabase CLI
npm install -g supabase

# Авторизуйтесь
supabase login

# Загрузите все файлы одной командой
./upload-to-supabase.sh
```

## 🎯 Минимальные шаги для деплоя

### 1. Supabase (один раз, 5 минут)

```
1. Создать проект на supabase.com
2. Создать PUBLIC bucket "pm-sim-assets"
3. Загрузить файлы в структуре:
   pm-sim-assets/game/
   ├── *.json (4 файла)
   └── *-icons/ (3 папки с иконками)
4. Скопировать Project URL и anon key
```

### 2. GitHub (один раз, 2 минуты)

```bash
git init
git add .
git commit -m "PM Simulator ready for deployment"
git remote add origin <your-github-repo>
git push -u origin main
```

### 3. Cloudflare Pages (один раз, 3 минуты)

```
1. dash.cloudflare.com → Workers & Pages → Create
2. Connect to Git → выбрать репозиторий
3. Build settings:
   - Build command: npm run build
   - Output directory: dist
4. Environment Variables (Production):
   - VITE_SUPABASE_URL = <from step 1>
   - VITE_SUPABASE_ANON_KEY = <from step 1>
   - VITE_SUPABASE_BUCKET = pm-sim-assets
   - VITE_SUPABASE_BASE_PATH = game/
5. Save and Deploy
```

## 🎉 После деплоя

### Игра доступна по адресу:
```
https://your-project.pages.dev
```

### Редактирование без редеплоя:

1. **Изменить баланс игры:**
   - Supabase Dashboard → Storage → `pm-sim-assets/game/config.json`
   - Редактировать → Сохранить
   - Игроки получат обновления при refresh

2. **Добавить новые задачи:**
   - Редактировать `departments.json` в Supabase
   - Изменения применяются мгновенно

3. **Изменить авторов/приоритеты:**
   - Редактировать соответствующие JSON файлы
   - Можно менять сообщения, добавлять новых авторов

### Обновление кода игры:

```bash
# Внесите изменения в код
git add .
git commit -m "Update game"
git push

# Cloudflare автоматически пересоберет и задеплоит!
```

## 📚 Дополнительная документация

После деплоя вам могут понадобиться:

- **[CONFIG_GUIDE.md](./CONFIG_GUIDE.md)** - как настроить баланс игры
- **[SUPABASE_SETUP.md](./SUPABASE_SETUP.md)** - детальная настройка Supabase
- **[README.md](./README.md)** - общая информация о проекте

## 🔍 Проверка корректности деплоя

После деплоя проверьте:

1. ✅ Игра загружается (нет белого экрана)
2. ✅ Стартовый экран отображается
3. ✅ Можно ввести имя и начать игру
4. ✅ Карточки задач появляются
5. ✅ Иконки департаментов отображаются
6. ✅ Drag & drop работает
7. ✅ Лидерборд сохраняет результаты
8. ✅ Нет ошибок в консоли (F12)

### Тест редактирования конфигов:

1. Откройте `config.json` в Supabase
2. Измените `basePoints` с 100 на 999
3. Сохраните
4. Перезагрузите игру (Ctrl+Shift+R)
5. Проверьте что начисляется 999+ очков
6. Верните обратно на 100

## 🆘 Если что-то не работает

### Игра не загружается
```
1. Откройте консоль (F12)
2. Посмотрите ошибки
3. Проверьте:
   - Environment variables установлены в Cloudflare
   - Supabase bucket публичный
   - Файлы загружены в правильную структуру
```

### 404 Not Found на конфигах
```
Проверьте структуру в Supabase:
✅ pm-sim-assets/game/config.json
❌ pm-sim-assets/config.json (неправильно)
```

### CORS errors
```
Bucket должен быть PUBLIC!
Storage → pm-sim-assets → Settings → Make public
```

## 💡 Советы

1. **Используйте Preview deployments** - Cloudflare создает отдельный URL для каждого PR
2. **Тестируйте локально** перед загрузкой конфигов в Supabase
3. **Делайте бэкапы** конфигов перед большими изменениями
4. **Мониторьте квоты** Supabase (но 50GB/месяц более чем достаточно)

## 🎮 Готово к игре!

После выполнения всех шагов у вас будет:

- ✅ Работающая игра на Cloudflare Pages
- ✅ Редактируемые конфиги в Supabase Storage  
- ✅ Автоматический деплой при push в GitHub
- ✅ Возможность менять баланс без остановки игры
- ✅ Лидерборд с сохранением результатов

**Следующие шаги:**
1. Поделитесь ссылкой с игроками
2. Соберите фидбэк
3. Настройте баланс через `config.json`
4. Добавьте новые задачи и департаменты

---

**Удачного деплоя! 🚀**

_Если возникнут вопросы - все ответы в [DEPLOYMENT.md](./DEPLOYMENT.md)_

