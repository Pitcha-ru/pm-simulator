# Примечания для разработчиков

## Архитектура кода

### Модули

Игра построена по модульному принципу:

1. **main.js** - Главный контроллер
   - Инициализация игры
   - Основной игровой цикл
   - Обработка событий

2. **gameState.js** - Управление состоянием
   - Хранит текущее состояние игры
   - Управляет активными отделами
   - Создает задачи

3. **gameLogic.js** - Игровая логика
   - Математические формулы
   - Расчет очков, таймеров, вероятностей
   - Чистые функции без побочных эффектов

4. **ui.js** - Управление интерфейсом
   - Рендеринг элементов
   - Анимации
   - Обновление отображения

5. **dragDrop.js** - Drag & Drop
   - Поддержка мыши и тача
   - Определение drop target
   - Управление состоянием перетаскивания

6. **supabaseLoader.js** - Загрузка данных
   - Работа с Supabase Storage
   - Fallback на локальные файлы
   - Предзагрузка изображений

7. **utils.js** - Вспомогательные функции
   - Математические утилиты
   - Форматирование
   - Работа с вероятностями

---

## Ключевые формулы

### Активные отделы по уровню

```javascript
count = startActive + floor((level - 1) / everyLevels) * addCount
count = clamp(count, startActive, maxActive)
```

Пример: startActive=4, everyLevels=2, addCount=1
- Уровень 1-2: 4 отдела
- Уровень 3-4: 5 отделов
- Уровень 5-6: 6 отделов

### Вероятность приоритетов (easeIn curve)

```javascript
t = clamp((level - startAtLevel) / (endAtLevel - startAtLevel), 0, 1)
value = startValue + (endValue - startValue) * (t ^ exponent)
```

Чем выше exponent, тем резче рост кривой.

### Таймер задачи

```javascript
scaled = baseSeconds * (multiplierEachLevel ^ (level - 1))
scaled = max(minSeconds, scaled)
finalSeconds = scaled * priorityTimeMultipliers[priority]
```

Пример: baseSeconds=18, multiplier=0.95
- Уровень 1: 18.0s
- Уровень 5: 14.7s
- Уровень 10: 11.3s
- Уровень 20: 6.8s

### Расчет очков

```javascript
points = basePoints * priorityScoreMultipliers[priority]
timeBonus = maxBonus * linear_falloff(progress, 0.25, 1.0)
finalScore = (points + timeBonus) * streakMultiplier
```

Временной бонус:
- 0-25% времени: полный бонус (60 очков)
- 25-100% времени: линейное снижение до 0

### Множитель серии

```javascript
streakMultiplier += 0.05 // за каждый успех
streakMultiplier = min(streakMultiplier, 1.5) // макс 1.5x
// Сброс на 1.0 при ошибке или таймауте
```

---

## Логика уровней

- **Задач на уровень**: Фиксировано 8 задач
- **Максимальный уровень**: 30 (из config.json)
- При достижении 30 уровня игра завершается

Изменить количество задач: `gameLogic.js → shouldLevelUp()`

---

## Система приоритетов

Три приоритета: `regular`, `serious`, `critical`

Вероятности рассчитываются динамически:
1. Serious и Critical - по easeIn кривым
2. Regular = 1 - serious - critical
3. Нормализация чтобы сумма = 1

Приоритет влияет на:
- Время на задачу (множители 1.0, 0.85, 0.70)
- Очки (множители 1.0, 1.35, 1.90)

---

## Обработка drag & drop

### Desktop (мышь)
- mousedown → начало перетаскивания
- mousemove → обновление позиции
- mouseup → проверка drop target

### Mobile (тач)
- touchstart → начало
- touchmove → обновление (с passive: false для preventDefault)
- touchend → drop

### Hit detection
```javascript
element = document.elementFromPoint(x, y)
departmentBlock = element.closest('.department-block')
departmentId = departmentBlock.dataset.departmentId
```

---

## Анимации

CSS классы для состояний:
- `.dragging` - карточка перетаскивается
- `.success` - правильный ответ
- `.fail` - неправильный ответ (shake)
- `.timeout` - время истекло (fade out)
- `.drag-over` - подсветка отдела при hover

Анимации определены в `styles.css` через `@keyframes`.

---

## Работа с данными

### Загрузка

1. При наличии Supabase credentials → загрузка из Storage
2. Иначе → загрузка локальных файлов

### Структура gameData

```javascript
{
  departments: {
    departments: [{ id, name, active, iconURL, tasks[] }],
    meta: { ... }
  },
  config: { ... },
  taskAuthors: {
    author: [{ id, name, avatarURL }]
  },
  priorityComments: {
    priority_commentary: {
      regular: { icon, iconURL, title, messages[] },
      serious: { ... },
      critical: { ... }
    }
  }
}
```

### Резолв URL

- Локально: `/${path}`
- Supabase: `${SUPABASE_URL}/storage/v1/object/public/${BUCKET}/${BASE_PATH}${path}`

---

## Производительность

### Оптимизации

1. **Предзагрузка изображений**
   - Все иконки и аватары загружаются до показа игры
   - Предотвращает мерцание

2. **Таймер с 50ms интервалом**
   - Баланс между плавностью и производительностью
   - requestAnimationFrame не используется (не критично)

3. **Кеширование DOM элементов**
   - UIManager кеширует все элементы при инициализации
   - Избегаем повторных querySelector

4. **CSS transitions вместо JS анимаций**
   - Аппаратное ускорение
   - Плавность 60 FPS

---

## Расширения

### Добавление нового отдела

1. Добавьте в `departments.json`:
   ```json
   {
     "id": "new_dept",
     "name": "Новый отдел",
     "active": true,
     "iconFile": "icon_new_dept.png",
     "tasks": ["Задача 1", "Задача 2"]
   }
   ```

2. Добавьте иконку в `dep-icons/icon_new_dept.png`

3. Обновите `meta.departmentsTotal` и `meta.departmentsActive`

### Добавление нового приоритета

Требует изменения кода:
- `gameLogic.js` - добавить кривую вероятности
- `config.json` - добавить multipliers
- `priority_comments.json` - добавить messages
- CSS - возможно новые стили

### Изменение прогрессии уровней

Опция 1: Изменить количество задач на уровень
```javascript
// gameLogic.js → shouldLevelUp()
const TASKS_PER_LEVEL = 10; // было 8
```

Опция 2: Добавить систему очков
- Требует добавления полей в config.json
- Изменение логики в gameLogic.js

---

## Отладка

### Dev mode

```bash
npm run dev
```

Открыть консоль браузера:
- Ошибки загрузки
- Состояние игры
- События

### Полезные точки останова

1. `main.js → startNewTask()` - spawn задачи
2. `main.js → onTaskDrop()` - обработка drop
3. `gameLogic.js → calculateScore()` - расчет очков
4. `gameState.js → spawnTask()` - создание задачи

### Логирование

Добавьте в нужных местах:
```javascript
console.log('Current state:', this.gameState.getState());
console.log('Score result:', scoreResult);
```

---

## Тестирование

### Ручное тестирование

Чеклист:
- [ ] Загрузка игры
- [ ] Spawn первой задачи
- [ ] Drag & Drop работает (desktop)
- [ ] Touch работает (mobile)
- [ ] Правильный drop → очки, новая задача
- [ ] Неправильный drop → shake, возврат
- [ ] Timeout → fade, новая задача
- [ ] Level up после 8 задач
- [ ] Новые отделы на новом уровне
- [ ] Streak увеличивается
- [ ] Streak сбрасывается

### Балансировка

Используйте эти параметры для тестирования:

Сложнее:
- Уменьшить `baseSeconds` (меньше времени)
- Уменьшить `multiplierEachLevel` (быстрее падает время)
- Уменьшить `everyLevels` (быстрее растет сложность)

Проще:
- Увеличить `baseSeconds`
- Увеличить `minSeconds` (не падает ниже)
- Увеличить `everyLevels`

---

## Возможные улучшения

### MVP+1
- [ ] Звуковые эффекты
- [ ] Музыкальный фон
- [ ] Анимация level up
- [ ] Частицы при успехе
- [ ] Сохранение прогресса (localStorage)
- [ ] Таблица лидеров

### MVP+2
- [ ] Мультиязычность
- [ ] Темная тема
- [ ] Настройки (звук, сложность)
- [ ] Режим тренировки
- [ ] Статистика игрока
- [ ] Достижения

### Технические
- [ ] Unit тесты для gameLogic.js
- [ ] E2E тесты (Playwright)
- [ ] CI/CD pipeline
- [ ] Аналитика событий
- [ ] Мониторинг ошибок

---

## Известные ограничения

1. **Одна задача за раз**
   - MVP показывает только одну карточку
   - Для мульти-карточек нужны значительные изменения

2. **Фиксированные задачи на уровень**
   - 8 задач хардкодом
   - Для динамики нужно добавить в config.json

3. **Нет штрафов за ошибки**
   - Только сброс streak
   - Для штрафов нужно расширить config.json

4. **Нет паузы**
   - Таймер всегда идет
   - Добавить UI кнопку паузы

---

## Контакты и поддержка

При возникновении вопросов:
1. Проверьте `README.md` и `DEPLOYMENT.md`
2. Проверьте консоль браузера
3. Проверьте валидность JSON файлов

---

**Удачной разработки! 🚀**

