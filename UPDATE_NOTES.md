# Обновление - Стартовый экран и лидерборд

## ✅ Что исправлено и добавлено

### 1. **Исправлен Drag & Drop** ✅
**Проблема**: Карточка смещалась при клике, даже без перетаскивания

**Решение**: 
- Добавлен порог перемещения (5 пикселей)
- Теперь drag начинается только после движения мыши на 5+ пикселей
- При простом клике карточка остается на месте
- Состояние `isPreparing` отслеживает намерение drag

**Технические детали**:
```javascript
this.dragThreshold = 5; // пикселей

// При mousedown - подготовка
prepareDrag(card, x, y) {
    this.isPreparing = true;
    // Сохраняем начальную позицию
}

// При mousemove - проверка расстояния
if (distance > this.dragThreshold) {
    this.startDrag(); // Начинаем drag
}
```

---

### 2. **Стартовый экран** ✅

**Добавлено**:
- Экран приветствия перед игрой
- Поле ввода имени игрока
- Кнопка "Начать игру"
- Топ-5 игроков на стартовом экране
- Enter для быстрого старта

**Как работает**:
1. После загрузки показывается стартовый экран
2. Игрок вводит имя (или остается "Аноним")
3. Показывается текущий топ-5
4. Нажимает "Начать игру" или Enter
5. Игра начинается с введенным именем

---

### 3. **Система лидерборда** ✅

**Новый модуль**: `leaderboard.js`

**Функционал**:
- Сохранение результатов в `localStorage`
- Автоматическая сортировка по очкам
- Хранение до 100 результатов
- Показ топ-5 игроков
- Расчет места игрока

**Данные сохраняются**:
```javascript
{
    playerName: "Имя игрока",
    score: 12500,
    level: 15,
    tasksCompleted: 120,
    timestamp: 1735478400000,
    date: "29.12.2025"
}
```

---

### 4. **Обновленный Game Over экран** ✅

**Добавлено**:
- Имя игрока
- Место в рейтинге (#X)
- Топ-5 игроков с именами и очками
- Подсветка текущего игрока (если в топ-5)

**Отображается**:
```
Игрок: Александр
Место: #3

Финальный счёт: 12,500
Достигнутый уровень: 15
Задач выполнено: 120

🏆 Топ-5 игроков
#1 Мария      15,200
#2 Иван       13,800
#3 Александр  12,500  ← Подсвечено
#4 Ольга      11,000
#5 Петр       10,500
```

---

## 📁 Новые файлы

### `js/leaderboard.js`
Модуль управления лидербордом:
- `addScore()` - добавить результат
- `getTopScores(n)` - получить топ-N
- `getPlayerRank(score)` - место игрока
- `clearScores()` - очистить таблицу

---

## 🔄 Изменённые файлы

### `index.html`
- ✅ Добавлен стартовый экран
- ✅ Обновлен Game Over экран
- ✅ Добавлены поля лидерборда

### `styles.css`
- ✅ Стили для стартового экрана
- ✅ Стили для лидерборда
- ✅ Стили для ранга игрока
- ✅ Адаптивная таблица лидеров

### `js/dragDrop.js`
- ✅ Добавлен порог перемещения
- ✅ Состояние `isPreparing`
- ✅ Метод `prepareDrag()`
- ✅ Проверка расстояния перед drag

### `js/ui.js`
- ✅ Методы для стартового экрана
- ✅ Рендеринг лидерборда
- ✅ Обновленный `showGameOver()`
- ✅ `renderLeaderboard()` - универсальный метод

### `js/main.js`
- ✅ Импорт `Leaderboard`
- ✅ Логика стартового экрана
- ✅ Сохранение в лидерборд
- ✅ Показ ранга при Game Over
- ✅ Хранение имени игрока

---

## 🎮 Игровой процесс

### Старый:
1. Загрузка → сразу игра
2. Game Over → счёт
3. Restart

### Новый:
1. **Загрузка**
2. **Стартовый экран** ← новое
   - Ввод имени
   - Просмотр топ-5
3. **Игра**
4. **Game Over** ← обновлено
   - Имя игрока
   - Место в рейтинге
   - Топ-5 с подсветкой
5. **Restart** → возврат к стартовому экрану

---

## 💾 localStorage

**Ключ**: `pm-simulator-leaderboard`

**Формат**: JSON массив результатов

**Пример**:
```json
[
  {
    "playerName": "Мария",
    "score": 15200,
    "level": 18,
    "tasksCompleted": 144,
    "timestamp": 1735478400000,
    "date": "29.12.2025"
  },
  ...
]
```

**Очистка**:
```javascript
// В консоли браузера:
localStorage.removeItem('pm-simulator-leaderboard');
```

---

## 🎨 UI/UX улучшения

### Drag & Drop
- ✅ Нет смещения при клике
- ✅ Плавный старт drag
- ✅ Визуальная стабильность

### Стартовый экран
- ✅ Автофокус на поле ввода
- ✅ Enter для быстрого старта
- ✅ Красивая градиентная подложка
- ✅ Топ-5 сразу виден

### Лидерборд
- ✅ Подсветка текущего игрока
- ✅ Четкая нумерация (#1, #2...)
- ✅ Выделение имени и очков
- ✅ Скролл для длинных списков

---

## 🔧 Технические детали

### Drag threshold
```javascript
// dragDrop.js
this.dragThreshold = 5; // пикселей

const dx = currentX - startX;
const dy = currentY - startY;
const distance = Math.sqrt(dx * dx + dy * dy);

if (distance > this.dragThreshold) {
    startDrag(); // Начинаем перетаскивание
}
```

### Сохранение результата
```javascript
// main.js → onGameOver()
this.leaderboard.addScore(
    this.playerName,        // Имя
    this.gameState.score,   // Очки
    this.gameState.level,   // Уровень
    this.gameState.totalTasksCompleted  // Задач
);

const rank = this.leaderboard.getPlayerRank(score);
const topScores = this.leaderboard.getTopScores(5);
```

### Рендеринг лидерборда
```javascript
// ui.js
renderLeaderboard(container, scores, highlightScore) {
    scores.forEach((entry, index) => {
        const isHighlight = entry.score === highlightScore;
        // Рендер с подсветкой
    });
}
```

---

## 📊 Статистика изменений

- **Новых файлов**: 1 (`leaderboard.js`)
- **Изменено файлов**: 5
- **Новых строк кода**: ~350
- **Новых методов**: 10+
- **Новых UI элементов**: 7

---

## 🚀 Как протестировать

1. **Обновите страницу**: `http://localhost:3000/`

2. **Стартовый экран**:
   - Введите имя
   - Нажмите "Начать игру"

3. **Drag & Drop**:
   - Кликните на карточку - она НЕ двигается
   - Начните тащить - появляется клон
   - Оригинал остается на месте

4. **Лидерборд**:
   - Сыграйте несколько раз
   - Проверьте топ-5 на стартовом экране
   - Проверьте свое место на Game Over

5. **localStorage**:
   ```javascript
   // В консоли браузера
   JSON.parse(localStorage.getItem('pm-simulator-leaderboard'))
   ```

---

## ✅ Все работает!

Протестируйте обновления и дайте знать если что-то не так! 🎮

**Приятной игры!**

