# Исправление критических багов

## 🐛 Исправленные проблемы

### 1. ✅ Карточка смещается при клике

**Проблема**: 
При клике на карточку она смещалась вниз и вбок, даже без перетаскивания.

**Причина**:
CSS transitions на `transform` срабатывали при любом изменении, включая hover и активацию.

**Решение**:
```css
/* БЫЛО */
.task-card {
    transition: transform 0.15s ease, box-shadow 0.15s ease;
}

.task-card:hover {
    transform: translateY(-4px);  /* Это вызывало смещение */
}

/* СТАЛО */
.task-card {
    transition: box-shadow 0.15s ease;
    /* НЕТ transition для transform! */
    user-select: none;
    touch-action: none;
}

.task-card:hover {
    /* НЕТ transform! Только shadow */
    box-shadow: 0 8px 20px var(--color-shadow-hover);
}
```

**Результат**: 
- ✅ Карточка остается на месте при клике
- ✅ Hover эффект остался (тень)
- ✅ Drag начинается только при движении (порог 5px)

---

### 2. ✅ Все жизни сгорают сразу

**Проблема**: 
При timeout нескольких карточек одновременно сгорали все жизни и игра заканчивалась.

**Причина**:
Метод `checkTimeouts()` обрабатывал ВСЕ просроченные карточки в одном цикле:
```javascript
// БЫЛО
checkTimeouts() {
    const timedOutTasks = this.gameState.getTimedOutTasks();
    
    timedOutTasks.forEach(task => {
        this.onTaskTimeout(task);  // Теряли по жизни за каждую!
    });
}
```

**Решение**:
Обрабатывать только ОДНУ просроченную карточку за раз:
```javascript
// СТАЛО
checkTimeouts() {
    const timedOutTasks = this.gameState.getTimedOutTasks();
    
    // Обрабатываем только первую просроченную
    if (timedOutTasks.length > 0) {
        const task = timedOutTasks[0];
        this.onTaskTimeout(task);  // Теряем 1 жизнь
    }
}
```

**Результат**:
- ✅ За каждую просроченную карточку теряется ровно 1 жизнь
- ✅ Карточки обрабатываются последовательно
- ✅ Игра не заканчивается мгновенно

**Как работает**:
1. Карточка 1 истекает → теряем 1 жизнь → удаляем карточку
2. Через 500ms карточка 2 (если еще актуальна) → теряем 1 жизнь
3. И так далее...

---

### 3. ✅ Лидерборд: 5 раз один игрок

**Проблема**:
При каждой игре добавлялась новая запись, поэтому один игрок занимал все 5 мест.

**Пример проблемы**:
```
#1 Александр  12,500
#2 Александр  11,200
#3 Александр  10,800
#4 Александр   9,500
#5 Александр   8,300
```

**Причина**:
Каждый результат добавлялся как отдельная запись без проверки существующих:
```javascript
// БЫЛО
addScore(playerName, score, ...) {
    this.scores.push(entry);  // Просто добавляли
    this.scores.sort(...);
}
```

**Решение**:
Хранить только ЛУЧШИЙ результат каждого игрока:
```javascript
// СТАЛО
addScore(playerName, score, ...) {
    const name = playerName || 'Аноним';
    
    // Ищем существующую запись игрока
    const existingIndex = this.scores.findIndex(s => 
        s.playerName.toLowerCase() === name.toLowerCase()
    );
    
    if (existingIndex !== -1) {
        // Игрок существует - обновляем ТОЛЬКО если новый результат лучше
        if (score > this.scores[existingIndex].score) {
            this.scores[existingIndex] = entry;
        }
    } else {
        // Новый игрок - добавляем
        this.scores.push(entry);
    }
    
    this.scores.sort((a, b) => b.score - a.score);
}
```

**Результат**:
- ✅ Каждый игрок появляется в таблице ОДИН раз
- ✅ Хранится только лучший результат игрока
- ✅ Реальная конкуренция между разными игроками

**Пример после исправления**:
```
#1 Мария       15,200
#2 Александр   12,500  ← Только лучший результат
#3 Иван        11,800
#4 Ольга       10,300
#5 Петр         9,700
```

---

### 4. ✅ Случайный порядок департаментов

**Проблема**:
Департаменты всегда появлялись в одинаковом порядке (первые N из списка).

**Причина**:
```javascript
// БЫЛО
updateActiveDepartments() {
    const allActiveDepts = ...;
    const count = ...;
    
    // Всегда брали первые N
    this.activeDepartments = allActiveDepts.slice(0, count);
}
```

**Решение**:
Перемешивать департаменты перед выбором:
```javascript
// СТАЛО
updateActiveDepartments() {
    const allActiveDepts = ...;
    const count = ...;
    
    // Перемешиваем (Fisher-Yates shuffle)
    const shuffled = this.shuffleArray([...allActiveDepts]);
    
    // Берем случайные N департаментов
    this.activeDepartments = shuffled.slice(0, count);
}

shuffleArray(array) {
    const shuffled = [...array];
    for (let i = shuffled.length - 1; i > 0; i--) {
        const j = Math.floor(Math.random() * (i + 1));
        [shuffled[i], shuffled[j]] = [shuffled[j], shuffled[i]];
    }
    return shuffled;
}
```

**Результат**:
- ✅ При каждом старте игры департаменты в случайном порядке
- ✅ При повышении уровня новый департамент выбирается случайно
- ✅ Больше разнообразия в игре

**Пример**:
```
Игра 1, уровень 1: [Backend, SEO, Rules, Web]
Игра 2, уровень 1: [Casino, Payments, LK, BAN-Team]
Игра 1, уровень 3: [Backend, SEO, Rules, Web, O-team] ← случайно добавлен
```

---

## 📊 Технические детали

### Drag & Drop порог

Уже был реализован порог 5 пикселей, но CSS transitions перебивали его эффект:

```javascript
// dragDrop.js
this.dragThreshold = 5;

// При movement
const distance = Math.sqrt(dx * dx + dy * dy);
if (distance > this.dragThreshold) {
    startDrag(); // Только после 5px движения
}
```

Теперь работает правильно благодаря удалению CSS transitions.

### Timeout обработка

Таймер проверяет каждые 50ms:
```javascript
setInterval(() => {
    this.updateAllTimers();
    this.checkTimeouts();  // Обрабатывает max 1 карточку
}, 50);
```

Каждая просроченная карточка:
1. Удаляется из активных
2. Отнимает 1 жизнь
3. Показывает анимацию
4. Через 500ms следующая (если есть)

### Лидерборд уникальность

Сравнение без учета регистра:
```javascript
s.playerName.toLowerCase() === name.toLowerCase()
```

Это значит:
- "Александр" и "александр" - один игрок
- "МАРИЯ" и "Мария" - один игрок

### Shuffle алгоритм

Fisher-Yates shuffle - O(n) сложность, гарантирует равномерное распределение:
```javascript
for (let i = n-1; i > 0; i--) {
    j = random(0, i)
    swap(array[i], array[j])
}
```

---

## 🧪 Тестирование

### 1. Карточка при клике
- ✅ Кликните на карточку - она НЕ двигается
- ✅ Начните тащить - появляется клон
- ✅ Hover на карточку - только тень меняется

### 2. Жизни
- ✅ Пропустите 1 карточку - потеряете 1 жизнь
- ✅ Пропустите 3 карточки - Game Over
- ✅ Если несколько истекли - обрабатываются по очереди

### 3. Лидерборд
- ✅ Сыграйте несколько раз под одним именем
- ✅ В таблице останется только лучший результат
- ✅ Каждое имя встречается максимум 1 раз

### 4. Департаменты
- ✅ Перезапустите игру несколько раз
- ✅ Порядок департаментов разный
- ✅ При level up добавляется случайный

---

## 🎮 Игровой процесс после исправлений

```
1. Стартовый экран
   ↓
2. Игра начинается
   - Департаменты в случайном порядке ✅
   - Карточки НЕ смещаются при клике ✅
   ↓
3. Пропущена карточка
   - Теряется 1 жизнь ✅
   - Остальные карточки продолжают
   ↓
4. Пропущены ещё 2 карточки
   - По 1 жизни за каждую ✅
   - Game Over при потере всех 3 ✅
   ↓
5. Результат сохраняется
   - Только если это лучший результат игрока ✅
   - Таблица показывает уникальных игроков ✅
```

---

## ✅ Все баги исправлены!

Обновите страницу и протестируйте:
- Карточки стабильные
- Жизни тратятся правильно
- Лидерборд корректный
- Департаменты случайные

**Приятной игры!** 🎮

