# 🚀 Быстрый старт

## Запуск игры локально (за 2 минуты)

1. **Установите зависимости**:
   ```bash
   npm install
   ```

2. **Запустите игру**:
   ```bash
   npm run dev
   ```

3. **Откройте в браузере**: 
   - Автоматически откроется `http://localhost:3000`
   - Или откройте вручную

**Готово!** 🎮 Игра работает с локальными JSON файлами.

---

## Деплой на Cloudflare (за 5 минут)

### Способ 1: Прямая загрузка (самый простой)

1. **Соберите проект**:
   ```bash
   npm run build
   ```
   
   (Это создаст папку `dist/` с игрой и скопирует все необходимые файлы)

2. **Зайдите в Cloudflare Pages**:
   - https://dash.cloudflare.com
   - Pages → Create a project → Upload assets

3. **Загрузите папку `dist/`**:
   - Перетащите всю папку `dist/` в окно загрузки
   - Дождитесь завершения

4. **Готово!** Игра развернута и доступна по URL от Cloudflare

---

### Способ 2: Через Git (автоматические обновления)

1. **Создайте Git репозиторий** (если еще не создан):
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   ```

2. **Загрузите на GitHub/GitLab**:
   ```bash
   git remote add origin https://github.com/your-username/pm-simulator.git
   git push -u origin main
   ```

3. **Подключите к Cloudflare Pages**:
   - Pages → Create a project → Connect to Git
   - Выберите репозиторий
   - Build settings:
     - Build command: `npm run build`
     - Build output: `dist`
   - Deploy

4. **Готово!** При каждом push игра будет обновляться автоматически

---

## Как играть

1. **Цель**: Перетащить карточку задачи в правильный отдел
2. **Управление**: 
   - Мышь: Захватите карточку и перетащите
   - Тач: Нажмите и перетащите пальцем
3. **Очки**: 
   - Базовые очки за задачу
   - Бонус за скорость
   - Множитель серии (увеличивается с каждой правильной задачей)
4. **Уровни**: Каждые 8 задач - новый уровень
5. **Сложность**: С каждым уровнем появляются новые отделы и меньше времени

---

## Изменение контента игры

### Редактирование задач и отделов

Откройте `departments.json` и измените:
- `name` - название отдела
- `tasks[]` - список задач отдела
- `active` - включен ли отдел (true/false)

### Балансировка сложности

Откройте `config.json` и настройте:
- `timers.regular.baseSeconds` - базовое время (18 сек)
- `scoring.basePoints` - базовые очки (100)
- `levels.maxLevels` - количество уровней (30)
- `departments.startActive` - отделов на старте (4)

### Изменение авторов

Откройте `task_authors.json`:
- Добавьте новых авторов
- Загрузите их аватары в `authors-icons/`

### Изменение комментариев

Откройте `priority_comments.json`:
- Отредактируйте сообщения для каждого приоритета

**После изменений**:
- Для локальной игры: просто перезагрузите страницу
- Для деплоя: запустите `npm run build` и загрузите заново

---

## Использование Supabase (опционально)

Если хотите обновлять контент без редеплоя:

1. Создайте бесплатный аккаунт на https://supabase.com
2. Создайте публичный Storage bucket
3. Загрузите JSON и иконки
4. Настройте переменные окружения в Cloudflare Pages

Подробности: см. `DEPLOYMENT.md`

---

## Структура проекта

```
pm-simulator/
├── index.html              # Главная страница
├── styles.css              # Все стили
├── js/                     # JavaScript модули
│   ├── main.js            # Главный контроллер
│   ├── gameState.js       # Состояние игры
│   ├── gameLogic.js       # Логика (очки, уровни, таймеры)
│   ├── dragDrop.js        # Перетаскивание
│   ├── ui.js              # Интерфейс
│   ├── supabaseLoader.js  # Загрузка данных
│   └── utils.js           # Утилиты
├── *.json                  # Игровые данные
└── *-icons/               # Иконки и аватары
```

---

## Команды

- `npm run dev` - Запуск для разработки
- `npm run build` - Сборка для продакшна (+ копирование assets)
- `npm run preview` - Предпросмотр продакшн сборки
- `npm run copy-assets` - Копировать assets в dist/ (если нужно)

---

## Поддержка

Проблемы или вопросы? Проверьте:
- `README.md` - полная документация
- `DEPLOYMENT.md` - подробное руководство по деплою
- `pm-simulator.md` - техническое задание

---

**Приятной игры! 🎮**

