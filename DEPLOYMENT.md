# Деплой на Cloudflare Pages с Supabase Storage

Это руководство покажет, как развернуть игру на Cloudflare Pages с хранением конфигураций в Supabase Storage для редактирования без повторного деплоя.

## 📋 Что нужно

1. Аккаунт [Supabase](https://supabase.com) (бесплатный план подходит)
2. Аккаунт [Cloudflare](https://cloudflare.com) (бесплатный план подходит)
3. Git репозиторий (GitHub, GitLab, или Bitbucket)

---

## Часть 1️⃣: Настройка Supabase Storage

### 1. Создайте проект в Supabase

1. Зайдите на https://supabase.com/dashboard
2. Нажмите **"New Project"**
3. Заполните данные проекта и дождитесь создания (~2 минуты)

### 2. Создайте Storage Bucket

1. В боковом меню выберите **"Storage"**
2. Нажмите **"Create a new bucket"**
3. Настройки:
   - **Name:** `pm-sim-assets`
   - **Public bucket:** ✅ Включите (чтобы файлы были доступны без аутентификации)
4. Нажмите **"Create bucket"**

### 3. Загрузите файлы игры в Storage

1. Откройте созданный bucket `pm-sim-assets`
2. Создайте папку `game/` (нажмите "Create folder")
3. Загрузите все файлы в структуре:

```
pm-sim-assets/
└── game/
    ├── config.json
    ├── departments.json
    ├── task_authors.json
    ├── priority_comments.json
    ├── dep-icons/
    │   ├── icon_backend.png
    │   ├── icon_ban_team.png
    │   ├── icon_casino.png
    │   ├── icon_lk.png
    │   ├── icon_o_team.png
    │   ├── icon_payments.png
    │   ├── icon_rules.png
    │   ├── icon_seo.png
    │   ├── icon_sys_admins.png
    │   ├── icon_web.png
    │   └── icon_welcome.png
    ├── priority-icons/
    │   ├── critical.png
    │   ├── regular.png
    │   └── serious.png
    └── authors-icons/
        ├── avatar_anna.png
        ├── avatar_dmitry.png
        ├── avatar_ekaterina.png
        ├── avatar_igor.png
        └── avatar_maria.png
```

**Способы загрузки:**

#### Вариант A: Через Web-интерфейс (проще)
- Зайдите в папку `game/`
- Нажмите **"Upload file"** и выберите файлы
- Создайте подпапки `dep-icons/`, `priority-icons/`, `authors-icons/` и загрузите иконки туда

#### Вариант B: Через Supabase CLI (быстрее для многих файлов)
```bash
# Установите Supabase CLI
npm install -g supabase

# Залогиньтесь
supabase login

# Загрузите файлы
supabase storage cp config.json supabase://pm-sim-assets/game/config.json
supabase storage cp departments.json supabase://pm-sim-assets/game/departments.json
supabase storage cp task_authors.json supabase://pm-sim-assets/game/task_authors.json
supabase storage cp priority_comments.json supabase://pm-sim-assets/game/priority_comments.json

# Загрузите папки с иконками
supabase storage cp -r dep-icons supabase://pm-sim-assets/game/dep-icons
supabase storage cp -r priority-icons supabase://pm-sim-assets/game/priority-icons
supabase storage cp -r authors-icons supabase://pm-sim-assets/game/authors-icons
```

### 4. Получите credentials Supabase

1. Перейдите в **Settings** > **API**
2. Скопируйте:
   - **Project URL** (например: `https://xxxxx.supabase.co`)
   - **anon/public key** (длинный JWT токен)

---

## Часть 2️⃣: Деплой на Cloudflare Pages

### 1. Подготовьте Git-репозиторий

```bash
# Инициализируйте git (если еще не сделано)
git init

# Создайте .gitignore
cat > .gitignore << EOF
node_modules/
dist/
.env
.DS_Store
EOF

# Добавьте файлы и закоммитьте
git add .
git commit -m "Initial commit: PM Simulator game"

# Создайте репозиторий на GitHub/GitLab и запушьте
git remote add origin <your-repo-url>
git push -u origin main
```

### 2. Создайте проект на Cloudflare Pages

1. Зайдите на https://dash.cloudflare.com/
2. Выберите **"Workers & Pages"** в боковом меню
3. Нажмите **"Create application"** > **"Pages"** > **"Connect to Git"**
4. Авторизуйте GitHub/GitLab и выберите ваш репозиторий
5. Настройте Build settings:

   ```
   Build command: npm run build
   Build output directory: dist
   Root directory: /
   ```

### 3. Настройте Environment Variables

На странице настройки проекта (или в **Settings** > **Environment variables**):

Добавьте переменные окружения для **Production**:

| Variable name | Value |
|--------------|-------|
| `VITE_SUPABASE_URL` | `https://xxxxx.supabase.co` |
| `VITE_SUPABASE_ANON_KEY` | Ваш anon/public key из Supabase |
| `VITE_SUPABASE_BUCKET` | `pm-sim-assets` |
| `VITE_SUPABASE_BASE_PATH` | `game/` |

⚠️ **Важно:** Используйте **Production** environment, не Preview!

### 4. Запустите деплой

1. Нажмите **"Save and Deploy"**
2. Дождитесь завершения сборки (~1-2 минуты)
3. Ваша игра будет доступна по адресу типа `https://pm-simulator.pages.dev`

---

## ✅ Проверка деплоя

После успешного деплоя:

1. Откройте URL вашего сайта
2. Игра должна загрузиться и отобразить стартовый экран
3. Проверьте консоль браузера (F12) - не должно быть ошибок загрузки

### Тест редактирования конфигов

1. Зайдите в Supabase Storage
2. Откройте `pm-sim-assets/game/config.json`
3. Измените какое-то значение (например, `basePoints`)
4. Сохраните файл
5. Перезагрузите игру (Ctrl+F5 для hard refresh)
6. Изменения должны примениться! 🎉

---

## 🔄 Как редактировать конфиги без редеплоя

### Редактирование через Supabase Dashboard

1. Зайдите в **Storage** > `pm-sim-assets` > `game/`
2. Выберите файл (например, `config.json`)
3. Нажмите три точки **"..."** > **"Download"**
4. Отредактируйте файл локально
5. Нажмите **"Upload"** > выберите обновленный файл
6. Подтвердите замену

### Редактирование JSON напрямую

Для некоторых файлов Supabase может показывать превью:
1. Откройте файл
2. Скопируйте содержимое
3. Отредактируйте в текстовом редакторе
4. Загрузите обратно через "Upload"

### Что можно редактировать:

- ✅ **config.json** - баланс игры, таймеры, очки
- ✅ **departments.json** - департаменты, задачи
- ✅ **task_authors.json** - авторы задач
- ✅ **priority_comments.json** - комментарии, сообщения
- ✅ **Иконки** - замена PNG файлов

### Кэширование

Браузеры могут кэшировать файлы. Для гарантированной загрузки обновлений:
- Попросите игроков сделать **Hard Refresh** (Ctrl+Shift+R или Cmd+Shift+R)
- Или настройте Cache-Control headers в Supabase Storage (опционально)

---

## 🚀 Обновление кода игры

Если вы меняете код (HTML/CSS/JS):

```bash
# Закоммитьте изменения
git add .
git commit -m "Update game logic"

# Запушьте
git push

# Cloudflare автоматически пересоберет и задеплоит! 🎉
```

---

## 🔧 Troubleshooting

### Игра не загружается / белый экран

1. Откройте консоль браузера (F12)
2. Проверьте наличие ошибок:
   - `Failed to load ... from Supabase` → проверьте правильность URL и bucket name
   - `CORS error` → убедитесь что bucket публичный
   - `401 Unauthorized` → проверьте anon key

### Конфиги не обновляются

1. Сделайте Hard Refresh (Ctrl+Shift+R)
2. Очистите кэш браузера
3. Проверьте что файл в Supabase действительно обновлен
4. Проверьте что используете правильный environment (Production, не Preview)

### Иконки не отображаются

1. Проверьте пути к файлам в Supabase (должны быть в подпапках `game/dep-icons/` и т.д.)
2. Убедитесь что имена файлов в JSON совпадают с именами в Storage
3. Проверьте что bucket публичный

---

## 📝 Дополнительные настройки (опционально)

### Кастомный домен

1. В Cloudflare Pages перейдите в **Custom domains**
2. Добавьте ваш домен
3. Следуйте инструкциям для настройки DNS

### Analytics

Cloudflare Pages предоставляет бесплатную аналитику:
- **Settings** > **Analytics** для просмотра статистики

### Preview deployments

Cloudflare автоматически создает preview URL для каждого PR в Git:
- Полезно для тестирования изменений перед мержем в main

---

## ✨ Готово!

Теперь у вас:
- ✅ Игра развернута на Cloudflare Pages
- ✅ Конфиги в Supabase Storage
- ✅ Можно редактировать баланс без редеплоя
- ✅ Автоматический деплой при пуше в Git

**Важно:** Храните ваш Supabase anon key в безопасности (но не переживайте - это публичный ключ, он предназначен для клиентских приложений).

Удачной игры! 🎮
